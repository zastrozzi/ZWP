# <Library Name>

Brief description of the library and its purpose.

## Usage

Install the package and import the module:

```ts
import { ExampleModule } from '@zwp/<library-name>';
```

Add `ExampleModule` to the `imports` array of your Angular module.

## Building

Run `nx build <library-name>` to build the library.

## Running unit tests

Run `nx test <library-name>` to execute the unit tests.

## Configuration

Document any required environment variables or configuration options here.
