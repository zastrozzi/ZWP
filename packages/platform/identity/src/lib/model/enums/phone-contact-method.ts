export enum PhoneContactMethod {
    sms = "sms",
    whatsapp = "whatsapp",
    voice = "voice"
}

export enum PhoneContactMethodLabel {
    sms = "SMS",
    whatsapp = "WhatsApp",
    voice = "Voice"
}