export enum CredentialType {
    emailPassword = "emailPassword",
    googleOAuth = "googleOAuth",
    siwa = "siwa"
}

export enum CredentialTypeLabel {
    emailPassword = "Email/Password",
    googleOAuth = "Google Account",
    siwa = "Apple ID"
}