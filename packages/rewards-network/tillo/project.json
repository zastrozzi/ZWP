{
    "name": "rewards-network.tillo",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "projectType": "library",
    "sourceRoot": "packages/rewards-network/tillo/src",
    "prefix": "rewards-network.tillo",
    "targets": {
        "build": {
            "executor": "@nrwl/angular:package",
            "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
            "options": {
                "project": "packages/rewards-network/tillo/ng-package.json"
            },
            "configurations": {
                "production": {
                    "tsConfig": "packages/rewards-network/tillo/tsconfig.lib.prod.json"
                },
                "development": {
                    "tsConfig": "packages/rewards-network/tillo/tsconfig.lib.json"
                }
            },
            "defaultConfiguration": "production"
        },
        "deploy": {
            "executor": "nx:run-commands",
            "options": {
                "command": "cd dist/packages/rewards-network/tillo && npm publish --access public"
            },
            "dependsOn": ["build"]

        },
        "test": {
            "executor": "@nrwl/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "packages/rewards-network/tillo/jest.config.ts",
                "passWithNoTests": true
            },
            "configurations": {
                "ci": {
                    "ci": true,
                    "codeCoverage": true
                }
            }
        },
        "lint": {
            "executor": "@nrwl/linter:eslint",
            "outputs": ["{options.outputFile}"],
            "options": {
                "lintFilePatterns": ["packages/rewards-network/tillo/**/*.ts", "packages/rewards-network/tillo/**/*.html"]
            }
        }
    },
    "tags": []
}
