export enum FaceValueLimits {
    lower = "lower",
    upper = "upper",
    minorUnit = "minor_unit"
}

export enum FaceValueLimitsLabel {
    lower = "Lower",
    upper = "Upper",
    minorUnit = "Minor Unit"
}