{
    "name": "rewards-network.merchant-net",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "projectType": "library",
    "sourceRoot": "packages/rewards-network/merchant-net/src",
    "prefix": "rewards-network.merchant-net",
    "targets": {
        "build": {
            "executor": "@nrwl/angular:package",
            "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
            "options": {
                "project": "packages/rewards-network/merchant-net/ng-package.json"
            },
            "configurations": {
                "production": {
                    "tsConfig": "packages/rewards-network/merchant-net/tsconfig.lib.prod.json"
                },
                "development": {
                    "tsConfig": "packages/rewards-network/merchant-net/tsconfig.lib.json"
                }
            },
            "defaultConfiguration": "production"
        },
        "deploy": {
            "executor": "nx:run-commands",
            "options": {
                "command": "cd dist/packages/rewards-network/merchant-net && npm publish --access public"
            },
            "dependsOn": ["build"]

        },
        "test": {
            "executor": "@nrwl/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "packages/rewards-network/merchant-net/jest.config.ts",
                "passWithNoTests": true
            },
            "configurations": {
                "ci": {
                    "ci": true,
                    "codeCoverage": true
                }
            }
        },
        "lint": {
            "executor": "@nrwl/linter:eslint",
            "outputs": ["{options.outputFile}"],
            "options": {
                "lintFilePatterns": ["packages/rewards-network/merchant-net/**/*.ts", "packages/rewards-network/merchant-net/**/*.html"]
            }
        }
    },
    "tags": []
}
