export enum PartnerStatus {
    draft = "draft",
    active = "active",
    hidden = "hidden",
    archived = "archived"
}

export enum PartnerStatusLabel {
    draft = "Draft",
    active = "Active",
    hidden = "Hidden",
    archived = "Archived"
}