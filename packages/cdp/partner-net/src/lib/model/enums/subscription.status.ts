export enum SubscriptionStatus {
    requested = 'requested',
    active = 'active',
    paused = 'paused',
    denied = 'denied'
}

export enum SubscriptionStatusLabel {
    requested = 'Requested',
    active = 'Active',
    paused = 'Paused',
    denied = 'Denied'
}